# scenarios/

**Status:** empty — lands in **v0.9** (see [../docs/ROADMAP.md](../docs/ROADMAP.md)).

Will hold the troubleshooting scenario engine: given a topology + injected
faults (from `labs/`), track objectives, validate the learner's fixes,
score the attempt, and surface hints/explanations. Consumes `topology/`,
`devices/`, `protocols/`, and `cli/` — no DOM.
