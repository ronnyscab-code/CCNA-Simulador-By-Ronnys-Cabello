/**
 * version.js
 *
 * Single source of truth for the build label shown in the UI. Bump this on
 * every change that gets rebuilt to the phone (V1 → V2 → V3…) so it's obvious
 * at a glance which build is actually running on the device.
 */
export const APP_VERSION = 'V1';
